<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
  <tr>
    <td><%= f.label :name %></td>
    <td><%= f.text_field :name %></td>
  </tr>
 <tr>
    <td>  <%= f.label :price %></td>
    <td><%= f.text_field :price %></td>
  </tr>

  <tr>
    <td><%= f.label :preorderprice, "Pre-Order Price" %></td>
    <td><%= f.text_field :preorderprice %></td>
  </tr>
  <tr>
    <td><%= f.label :preorderdate, "Pre-Orders Available Until" %> </td>
    <td><%= f.date_select :preorderdate %></td>
  </tr>

</table>
  <div class="field">
    <%= f.label :picture %><br />
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
